diff --git a/include/llama.h b/include/llama.h
index a311ac2..287830e 100644
--- a/include/llama.h
+++ b/include/llama.h
@@ -396,6 +396,15 @@ extern "C" {
         // a source/target/parent context
         // can be utilized in various ways, for example by sharing results or llama_memory between 2 contexts
         struct llama_context * ctx_other;
+
+        // [HYDRA M-1 spike] pipeline-shard layer window: run only transformer layers [il_start, il_end).
+        //   il_start > 0        => this shard consumes an injected boundary residual via batch.embd
+        //                          (embeddings input path) instead of token lookup.
+        //   il_end   < n_layer  => "boundary" shard: skip final norm + lm_head and emit the RAW
+        //                          residual entering layer il_end as embeddings (llama_get_embeddings*).
+        //   Defaults (0, -1) => whole model, unchanged behavior. See BLUEPRINT.md M-1 / spike/.
+        int32_t il_start;
+        int32_t il_end;   // -1 = n_layer
     };
 
     struct llama_model_tensor_override {
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index 3a469bc..6d66fdc 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -96,6 +96,8 @@ llama_context::llama_context(
     cparams.yarn_beta_fast          = params.yarn_beta_fast   >= 0.0f ? params.yarn_beta_fast   : hparams.yarn_beta_fast;
     cparams.yarn_beta_slow          = params.yarn_beta_slow   >= 0.0f ? params.yarn_beta_slow   : hparams.yarn_beta_slow;
     cparams.embeddings              = params.embeddings;
+    cparams.il_start                = params.il_start;   // [HYDRA M-1] pipeline-shard layer window
+    cparams.il_end                  = params.il_end;
     cparams.embeddings_nextn        = false;
     cparams.embeddings_nextn_masked = false;
     cparams.offload_kqv             = params.offload_kqv;
@@ -3468,6 +3470,8 @@ llama_context_params llama_context_default_params() {
         /*.sampler                     =*/ nullptr,
         /*.n_sampler                   =*/ 0,
         /*.ctx_other                   =*/ nullptr,
+        /*.il_start                    =*/ 0,   // [HYDRA M-1] full model by default
+        /*.il_end                      =*/ -1,  // [HYDRA M-1] -1 => n_layer
     };
 
     return result;
diff --git a/src/llama-cparams.h b/src/llama-cparams.h
index 58520ca..42fc8c4 100644
--- a/src/llama-cparams.h
+++ b/src/llama-cparams.h
@@ -20,6 +20,9 @@ struct llama_cparams {
 
     int32_t  nextn_layer_offset = 0;
 
+    int32_t  il_start = 0;   // [HYDRA M-1] pipeline-shard layer window [il_start, il_end)
+    int32_t  il_end   = -1;  // [HYDRA M-1] -1 => n_layer
+
     float rope_freq_base;
     float rope_freq_scale;
 
diff --git a/src/models/llama.cpp b/src/models/llama.cpp
index 4bfebc8..4be32de 100644
--- a/src/models/llama.cpp
+++ b/src/models/llama.cpp
@@ -123,7 +123,14 @@ llama_model_llama::graph<embed>::graph(const llama_model & model, const llm_grap
 
     ggml_tensor * inp_out_ids = build_inp_out_ids();
 
-    for (int il = 0; il < n_layer; ++il) {
+    // [HYDRA M-1] pipeline-shard layer window [l0, l1). Defaults (0, n_layer) => whole model.
+    // l0 > 0        : inpL is an injected boundary residual (batch.embd path), not token lookup.
+    // l1 < n_layer  : this shard emits the raw residual entering layer l1 instead of logits.
+    const int32_t l0 = cparams.il_start > 0 ? cparams.il_start : 0;
+    const int32_t l1 = (cparams.il_end < 0 || cparams.il_end > (int32_t) n_layer)
+                       ? (int32_t) n_layer : cparams.il_end;
+
+    for (int il = l0; il < l1; ++il) {
         res->t_layer_inp[il] = inpL;
 
         ggml_tensor * inpSA = inpL;
@@ -171,7 +178,7 @@ llama_model_llama::graph<embed>::graph(const llama_model & model, const llm_grap
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, kq_scale, il);
             cb(cur, "attn_out", il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == l1 - 1 && inp_out_ids) {   // [HYDRA M-1] shard's last layer (l1-1), not n_layer-1
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -227,12 +234,14 @@ llama_model_llama::graph<embed>::graph(const llama_model & model, const llm_grap
         inpL = cur;
     }
     cur = inpL;
+    ggml_tensor * hydra_boundary = cur;   // [HYDRA M-1] raw residual entering layer l1
 
     cur = build_norm(cur,
             model.output_norm, NULL,
             LLM_NORM_RMS, -1);
 
     cb(cur, "result_norm", -1);
+    ggml_tensor * normed = cur;
     res->t_embd = cur;
 
     if constexpr (!embed) {
@@ -241,6 +250,10 @@ llama_model_llama::graph<embed>::graph(const llama_model & model, const llm_grap
 
         cb(cur, "result_output", -1);
         res->t_logits = cur;
+
+        // [HYDRA M-1] a boundary shard (l1 < n_layer) exposes the RAW pre-norm residual as
+        // embeddings (read via llama_get_embeddings*) for injection into the downstream shard.
+        res->t_embd = (l1 < (int32_t) n_layer) ? hydra_boundary : normed;
     }
 
     ggml_build_forward_expand(gf, cur);
diff --git a/src/models/qwen2.cpp b/src/models/qwen2.cpp
index e9c2ea8..7bf0354 100644
--- a/src/models/qwen2.cpp
+++ b/src/models/qwen2.cpp
@@ -68,7 +68,14 @@ llama_model_qwen2::graph::graph(const llama_model & model, const llm_graph_param
 
     ggml_tensor * inp_out_ids = build_inp_out_ids();
 
-    for (int il = 0; il < n_layer; ++il) {
+    // [HYDRA M-1] pipeline-shard layer window [l0, l1). Defaults (0, n_layer) => whole model.
+    // l0 > 0        : inpL is an injected boundary residual (batch.embd path), not token lookup.
+    // l1 < n_layer  : this shard emits the raw residual entering layer l1 instead of logits.
+    const int32_t l0 = cparams.il_start > 0 ? cparams.il_start : 0;
+    const int32_t l1 = (cparams.il_end < 0 || cparams.il_end > (int32_t) n_layer)
+                       ? (int32_t) n_layer : cparams.il_end;
+
+    for (int il = l0; il < l1; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -103,7 +110,7 @@ llama_model_qwen2::graph::graph(const llama_model & model, const llm_graph_param
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == l1 - 1 && inp_out_ids) {   // [HYDRA M-1] shard's last layer (l1-1), not n_layer-1
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -133,13 +140,14 @@ llama_model_qwen2::graph::graph(const llama_model & model, const llm_graph_param
         inpL = cur;
     }
     cur = inpL;
+    ggml_tensor * hydra_boundary = cur;   // [HYDRA M-1] raw residual entering layer l1
 
     cur = build_norm(cur,
             model.output_norm, NULL,
             LLM_NORM_RMS, -1);
 
     cb(cur, "result_norm", -1);
-    res->t_embd = cur;
+    ggml_tensor * normed = cur;
 
     // lm_head
     cur = build_lora_mm(model.output, cur, model.output_s);
@@ -150,5 +158,10 @@ llama_model_qwen2::graph::graph(const llama_model & model, const llm_graph_param
     cb(cur, "result_output", -1);
     res->t_logits = cur;
 
+    // [HYDRA M-1] a boundary shard (l1 < n_layer) exposes the RAW pre-norm residual as
+    // embeddings (read via llama_get_embeddings*), for injection into the downstream shard.
+    // The final shard exposes the normed hidden state as usual.
+    res->t_embd = (l1 < (int32_t) n_layer) ? hydra_boundary : normed;
+
     ggml_build_forward_expand(gf, cur);
 }
