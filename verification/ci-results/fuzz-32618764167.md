# Parser fuzzing — leg 1 receipt (M4·1 c)

**Run:** [32618764167](https://github.com/PavanManchikatla/Hydra/actions/runs/32618764167) ·
`workflow_dispatch`, `seconds=180` per shard · **2026-08-23** · sha `581da25` · ubuntu-latest,
release profile.

> **⚑⚑ WHICH PARSER THIS RECEIPT PROTECTS — CORRECTION, 2026-08-23 (audit H6).**
>
> This receipt says "the GGUF parser". **It means `hydra_modelsvc::gguf` — the parser the OFFLINE
> SPLITTER runs.** A worker does **not** run it. Until 2026-08-23 the worker hashed the shard file
> and handed the **path** to `llama.cpp`, whose own GGUF parser had never been fuzzed by this
> project at all. So this receipt's CPU-hours were protecting a program that does not execute on
> the load path, and the receipt did not say so — **overstating by silence, which is §7.31's shape
> in the evidence layer.**
>
> **What is true of the numbers below:** they are real, and they are about the splitter's reader.
> **What they never established:** anything about the parser that maps a shard into a worker.
>
> **Since 2026-08-23:** an eighth target, **`vendored-gguf`**, drives `gguf_init_from_file`; and the
> worker now runs the hardened parser itself before the engine sees the path (§7.43). On its first
> sweep the raw vendored probe **aborted** (`SIGABRT` in `gguf_init_from_file_ptr`, seed 1
> iteration 350) — an upstream `llama.cpp` defect, recorded in §7.43 and owed upstream. Note also
> that **CI cannot run that target yet** (audit L1: CI never builds the real engine), so its status
> there is *unavailable*, never *green*.

**Rule 16:** this file is the evidence. Every line below is quoted verbatim from the runner log's
`Fuzz (shard N)` step. The job's green check-mark is not what makes this GREEN — the `verdict=`
lines are.

---

## Verdict

**24 / 24 legs GREEN. 0 crashes. 1 967 017 984 cases. 1440 CPU-seconds = 0.400 CPU-hours.**

```
FUZZ SUMMARY targets=3 cpu_seconds=180.0 crashes=0 verdict=GREEN
```

(one `FUZZ SUMMARY` per shard; all eight identical, hence the single deduplicated line)

The workflow's own classify step, applied per shard:

```
verdict=GREEN (FUZZ SUMMARY targets=3 cpu_seconds=180.0 crashes=0 verdict=GREEN)
```

## Per-leg verdicts (8 shards × 3 targets), verbatim

```
target=gguf         seed=1000003 iterations=54887424  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=gguf         seed=2000006 iterations=57053184  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=gguf         seed=3000009 iterations=77271040  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=gguf         seed=4000012 iterations=57916416  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=gguf         seed=5000015 iterations=57063424  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=gguf         seed=6000018 iterations=52120576  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=gguf         seed=7000021 iterations=64569344  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=gguf         seed=8000024 iterations=56912896  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=1000003 iterations=127670272 cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=2000006 iterations=109010944 cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=3000009 iterations=183797760 cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=4000012 iterations=135080960 cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=5000015 iterations=110055424 cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=6000018 iterations=92536832  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=7000021 iterations=147983360 cpu_seconds=60.0 crashes=0 verdict=GREEN
target=frame-header seed=8000024 iterations=108818432 cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=1000003 iterations=55916544  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=2000006 iterations=53204992  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=3000009 iterations=81641472  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=4000012 iterations=59744256  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=5000015 iterations=51560448  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=6000018 iterations=49880064  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=7000021 iterations=68519936  cpu_seconds=60.0 crashes=0 verdict=GREEN
target=wire-body    seed=8000024 iterations=53801984  cpu_seconds=60.0 crashes=0 verdict=GREEN
```

**No `CRASH` line appears anywhere in the run** (`grep -c 'CRASH target=' → 0`), which is the
condition the classify step keys on. A crash would have printed its full `(seed, iteration)` replay
coordinates before the verdict, so a RED verdict here would always be actionable.

| Target | Cases | What it parses |
|---|---:|---|
| `gguf` | 477 794 304 | A **downloaded model file** — report Addendum 2 §D1's untrusted input |
| `frame-header` | 1 014 953 984 | The `HYFR` header, the **pre-allocation** path |
| `wire-body` | 474 269 696 | FlatBuffers body + the F1 fence, reached from the network |
| **Total** | **1 967 017 984** | |

Every shard ran a **distinct seed** (`shard × 1000003`), so the 1.97 billion cases are 1.97 billion
*different* cases — parallelism bought coverage, not repetition. That is the reason the shard seed
is derived from the shard index rather than shared.

---

## Running total toward the 24 CPU-hour DoD

| Leg | Run | Trigger | CPU-hours | Cases | Crashes | Cumulative CPU-h |
|---|---|---|---:|---:|---:|---:|
| 1 | [32618764167](https://github.com/PavanManchikatla/Hydra/actions/runs/32618764167) | dispatch, 180 s/shard | 0.400 | 1 967 017 984 | 0 | 0.400 |
| 2 | [32618760313](https://github.com/PavanManchikatla/Hydra/actions/runs/32618760313) | push `581da25`, 900 s/shard | 2.000 | 9 355 326 464 | 0 | **2.400** |

**2.400 / 24 CPU-hours. The DoD is NOT met, and no receipt in this directory claims it is.** Weekly
legs run 8 shards × 900 s ≈ 2.0 CPU-hours each, so **~11 further legs** reach the bar. Tracked as a
standing owed item in PROJECT_STATE §8, and **every future leg appends a row to this table** — leg 2's
own receipt is [`fuzz-32618760313.md`](fuzz-32618760313.md).

---

## What this receipt does and does not establish

**Does:** the three parsers survived nearly two billion structure-aware hostile inputs without a
panic, an abort, or an unbounded allocation — after the two CVE-class defects the same fuzzer found
in its first second of existence were fixed (§7.28 D2, D3).

**Does not:**

* **It is not 24 CPU-hours.** It is 0.4, and the DoD stays open.
* **It is not coverage-guided fuzzing.** `hydra-fuzz` is deterministic and structure-aware; it has
  no feedback loop telling it which mutations reach new code. A libFuzzer/AFL campaign would, given
  time, reach deeper. The trade is deliberate — `(seed, iteration)` reproduction and a stable-Rust
  CI arm this project can watch run (standing rule 12) — and `cargo-fuzz` over the same entry points
  is an owed additive upgrade (§8).
* **It says nothing about the engine's own GGUF path.** `hydra-modelsvc` is the pure-Rust parser
  Hydra wrote; `llama.cpp` has its own, reached when a shard is actually loaded, and it is not
  fuzzed here.
* **Absence of a crash is not absence of a bug.** It is absence of *this* class — panics, aborts and
  unbounded allocations — under *these* generators.

## Reproducing any case

Every case is a pure function of `(target, seed, iteration)`:

```
cargo run --release -p hydra-fuzz --bin hydra-fuzz -- --target gguf --seed 1000003 --replay 412345
```

`--replay` restores the default panic hook, so the operator gets the backtrace the campaign
deliberately suppresses (one backtrace per case would be gigabytes of log and no information).
