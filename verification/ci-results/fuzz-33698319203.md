# Parser fuzzing — leg 12 receipt (M4·1 c)

**Run:** [33698319203](https://github.com/PavanManchikatla/Hydra/actions/runs/33698319203) · `event=workflow_dispatch` ·
2026-09-03T00:10:00Z · sha `9038a1d` · 8 shards · ubuntu-latest, release profile.

**Basis (rule 24): dispatched, RESTRICTED to targets=vendored-gguf, 8 shards × 2700 s — the vendored-only leg the 2026-09-02 ruling ordered first; classified against only the target it asked for (rule 24 basis stated).** shard-seconds=2700 trigger=workflow_dispatch targets=vendored-gguf

**Rule 16:** every line below is quoted from `gh run view 33698319203 --log`, de-duplicated across the eight
`fuzz (N)` jobs, script echo excluded. Engine lines: engine-pin=c00bcebf79cbf95be22aa711d485a287914672c5; engine=REAL (no stub message in the build log).

## Verdict

**GREEN.** Classify line (one per shard, identical) and the driver's summary:

```
verdict=GREEN (FUZZ SUMMARY targets=1 unavailable=0 cpu_seconds=2700.0 crashes=0 verdict=GREEN)
FUZZ SUMMARY targets=1 unavailable=0 cpu_seconds=2700.0 crashes=0 verdict=GREEN
```

No `CRASH` line and no abort marker in the run.

### Per target, summed across the shards (8 `target=` lines)

| Target | Iterations | CPU-s | Crashes | shard-lines |
|---|---:|---:|---:|---:|
| `vendored-gguf` | 3 825 900 544 | 21600.0 | 0 | 8 |

**Total: 3 825 900 544 cases in 21600.0 CPU-seconds = 6.000 CPU-hours.**


## Cumulative toward the 24-CPU-hour DoD

Previous cumulative (legs 1–11): **28.150 CPU-h, 161 152 325 632 cases.**
This leg: **6.000 CPU-h, 3 825 900 544 cases.**
**Cumulative after leg 12: 34.150 / 24 CPU-hours, 164 978 226 176 cases, 0 crashes — THE 24-HOUR NUMBER IS MET.**

The full leg-by-leg table lives in `fuzz-33491900881.md` (legs 1–7) and is continued by this row:

| Leg | Run | Trigger | CPU-h | Cases | Crashes | Cumulative |
|---|---|---|---:|---:|---:|---:|
| 12 | [33698319203](https://github.com/PavanManchikatla/Hydra/actions/runs/33698319203) | workflow_dispatch (dispatched, RESTRICTED to targets=vendored-gguf, 8 shards × 2700 s) | 6.000 | 3 825 900 544 | 0 | **34.150** |

## The restriction, stated (rule 24)

This leg was dispatched with the `targets=vendored-gguf` input the 2026-09-02 ruling asked for, so **the seven
pure-Rust parsers were NOT run on this leg** — every one of its 6.000 CPU-hours is on the vendored parser, and the
classify step required only the target it asked for (`FUZZ SUMMARY targets=1 unavailable=0`). It exists because
the per-parser basis (M4-GATE row 3(c), §7.74) showed the parser a worker actually loads through carried the fewest
hours of the eight.

**Per parser after this leg, from every receipt's own `cpu_seconds` (legs 1–12):** `gguf` 4.229 h · `frame-header`
4.229 h · `wire-body` 4.229 h · `manifest` / `bootstrap` / `wal-record` / `boundary-record` 3.429 h each ·
**`vendored-gguf` 6 300 s + 21 600 s = 27 900 s = 7.750 h** — summed 122 948 s = 34.152 h (34.150 with per-leg rounding).
No single parser has 24 hours; the parser a worker loads through now has the most.

**Also observed on this leg — the first with ggml logging silenced in the driver (§7.73):** each shard's job log is
**≈ 72 KB** (`gh run view --job … --log`, 71 960–71 968 bytes across the eight), where legs 9–11 produced
**≈ 85 MB per shard** and truncated the run-level log endpoint. The silencing is observed, not assumed.

## What this receipt does and does not establish

**Does:** ONE parser — `vendored-gguf`, `gguf_init_from_file` inside the pinned fork — survived 3.8 billion structure-aware hostile inputs on this leg without a
panic, abort, or unbounded allocation.

**Does not:** it is not coverage-guided; no timeout oracle and no allocation ceiling (§8 rule-19 gaps);
absence of a crash is absence of *this* class under *these* generators.
