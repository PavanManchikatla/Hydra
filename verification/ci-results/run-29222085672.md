# TLC run 29222085672 — rule-12 semantic log-read receipt

**Read 2026-07-13** from the GitHub Actions job consoles (not the run-status page).
**Workflow:** `.github/workflows/tlc.yml` `long` job · **headSha `9372ee1`** (the F-LIVENESS-FAIR
v0.10.4 model; unchanged at current HEAD, so `-recover` from these checkpoints stays valid per
PROJECT_STATE rule 13). Inner time-box `timeout 320m`, `-checkpoint 60`.

> **Why this receipt exists.** PROJECT_STATE rule 12: a results table may say GREEN only when the
> *log* says so, and a milestone flip gets a human-readable receipt. The hardened Classify step
> (558af49) writes an explicit `verdict=` per config; **this file quotes those verdicts verbatim.**

## Verdict lines (verbatim from each job's Classify step)

```
config=baseline-safety expect=clean     violated=0 complete=0 verdict=INCONCLUSIVE
config=baseline-live    expect=clean     violated=0 complete=0 verdict=INCONCLUSIVE
config=mut1             expect=violation  violated=1 complete=0 verdict=GREEN
config=mut3             expect=violation  violated=0 complete=0 verdict=INCONCLUSIVE
```

For the three INCONCLUSIVE legs, the Classify step also printed verbatim:

```
INCONCLUSIVE (time-boxed) — checkpoint uploaded; re-dispatch recover=true to continue. NOT green.
```

## What the run actually established

| Leg | Job ID | Wall | Verdict | Meaning |
|---|---|---|---|---|
| smoke | 86729104661 | 4m15s | ✓ | parse + Mut2/Mut4 smoke pass (unchanged) |
| baseline-safety | 86729104785 | 5h27m | **INCONCLUSIVE** | time-boxed at 320m; **NOT fixpoint** |
| baseline-live | 86729104682 | 5h24m | **INCONCLUSIVE** | time-boxed at 320m; **NOT a clean drain** |
| mut1 | 86729104675 | 1m22s | **GREEN (fired)** | temporal property violated |
| mut3 | 86729104672 | 5h28m | **INCONCLUSIVE** | time-boxed at 320m; **did NOT fire** |

Only **mut1 fired**. The three long-running legs each **time-boxed** (inner `timeout 320m`)
with large queues still undrained, and uploaded `-checkpoint 60` snapshots for `-recover`
continuation. **This run did NOT conclude the C2 / M1-full-flip gate.**

## Verbatim TLC markers (from the job consoles)

**baseline-safety** — last progress before the 320m kill (no "Model checking completed" line):
```
Progress(887) at 2026-07-13 08:56:47: 1,285,670,130 states generated, 327,894,772 distinct
states found, 380,964 states left on queue.
```

**baseline-live** — last progress before the 320m kill (no "Model checking completed" line):
```
Progress(60) at 2026-07-13 08:57:07: 91,427,131 states generated, 25,001,313 distinct
states found, 758,325 states left on queue.
```

**mut3** — last progress before the 320m kill (no "is violated" line anywhere):
```
Progress(998) at 2026-07-13 08:56:44: 1,513,905,668 states generated, 379,706,745 distinct
states found, 390,911 states left on queue.
```

**mut1** — the genuine counterexample (this is the one leg that concluded):
```
State 23: Stuttering
Finished checking temporal properties in 01s at 2026-07-13 03:38:40
1420399 states generated, 467199 distinct states found, 78603 states left on queue.
```
(Classify grep of the full `mut1.log` on the runner counted `violated=1` — the
`Temporal properties were violated` / `PostDecisionLoss` header is in the archived
`tlc-checkpoint-mut1` artifact; the job console only tails the last 20 lines, shown above.)

## Consequence

- **M1 full-flip: BLOCKED.** The mechanical protocol (§0e) requires baseline-safety→fixpoint,
  baseline-live→clean drain, and Mut3 fire — **none reached** here. Only Mut1 is green.
- **Continue via `recover=true`** on `tlc.yml` (checkpoints uploaded; model unchanged). That
  simultaneously (i) advances the three drains toward their verdicts and (ii) **exercises the
  `-recover` round-trip for the first time** — the untested-link verification the owner flagged
  as caveat (b), now observed rather than deferred.
- **Root cause of the prior misrecording (see PROJECT_STATE §7):** §0b read the run-status page
  (the ✓ marks and the matrix *labels* `...clean` / `...violation`, which are the **expected**
  outcomes, not results) instead of the Classify `verdict=` lines. Same failure class as
  F-LIVENESS-FAIR (§7.13). The 558af49 hardening worked — the receipt was present in the
  Classify output the whole time; it simply wasn't read.
