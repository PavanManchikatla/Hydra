# Receipt — run 32654396107 (TLC, H1/M13 amendment legs) — **COMPLETE. VERDICT: H1/M13 VERIFIED.**

**Dispatched:** 2026-08-23 17:18 UTC · `workflow_dispatch` on `7cf1445` (the H1 + M13 amendment
commit: `MaxAttempt`-bounded window + the new `ResetPreservesAttemptSpace` property). Read
2026-08-23 (fresh session) **via the per-job log API while the run was `in_progress`** — `gh run view
--log` refuses an in-progress run, so each completed job's log was fetched with
`gh api repos/…/actions/jobs/<id>/logs` and grepped for `verdict=`. Rule 13: the `.tla` changed, so no
prior checkpoint applies; every leg below ran fresh.

**Companion run [32654389806](https://github.com/PavanManchikatla/Hydra/actions/runs/32654389806)
(`push` on `7cf1445`) ran `smoke` ONLY — its `long` matrix is `skipped` on push. It therefore
supplies NO baseline and NO mutation verdicts.** §0(b)'s "both baselines at `complete=1
verdict=GREEN` are required" cannot be met from the push run by construction; the requirement rests
on the dispatch run alone.

## Per-leg Classify lines, verbatim (rule 16)

```
config=baseline-safety expect=clean violated=0 complete=1 verdict=GREEN
config=baseline-safety-fast expect=clean violated=0 complete=1 verdict=GREEN
config=mut1 expect=violation violated=1 complete=0 verdict=GREEN
config=mut3 expect=violation violated=1 complete=0 verdict=GREEN
config=mut3-fast expect=violation violated=1 complete=0 verdict=GREEN
config=baseline-live expect=clean violated=0 complete=1 verdict=GREEN
```

`smoke` (no `verdict=` line by design; its own OK lines):

```
OK: Mut2 tripped CaseBPure (rc=12)
OK: Mut4 tripped AbortFinality (rc=12)
OK: baseline-safety bounded run completed, no violation
```

**`baseline-live` (`BaselineLiveness.cfg`, expect clean) — read on completion, quoted above:
`violated=0 complete=1 verdict=GREEN`.** From the same log:

```
13586283 states generated, 3981633 distinct states found, 0 states left on queue.
Finished in 35min 56s at (2026-08-23 17:54:21)
```

**`0 states left on queue` is what makes this a CLEAN DRAIN rather than a time-box** — the
distinction §0(b) insists on, and the only condition under which a temporal property is meaningfully
evaluated. `BaselineLiveness.cfg`'s `PROPERTIES` block lists **`ResetPreservesAttemptSpace`** (with
`Progress`, `PostDecisionLoss`, `EventualService`), so the amendment's new property was checked over
the drained space and reported no violation.

## What this establishes, and what it does not

* **`baseline-safety` drained to fixpoint on the amended model** — `complete=1`. The H1 bounded
  window did not break safety.
* **Mut1 still fires. Mut3 STILL FIRES** (`violated=1`) — the §0(b) escalation condition ("Mut3 going
  quiet is an ESCALATION, not an explanation") is **not** triggered. Narrowing the attempt fence
  did **not** silence the mutation that exists to catch the fence's absence.
* **ESTABLISHED: `ResetPreservesAttemptSpace` holds on a clean drain.** `baseline-live` reached
  fixpoint (0 on queue) with no violation, so the M13 amendment's property was genuinely evaluated
  rather than skipped by an early stop. **H1/M13 is VERIFIED** — it is no longer "ratified and
  locally smoke-verified only".
* **The §0(b) acceptance conditions, item by item:** both baselines `complete=1 verdict=GREEN`
  (`baseline-safety` ✅, `baseline-live` ✅); **Mut1 still fires** ✅; **Mut3 still fires** ✅ (the
  escalation condition — "Mut3 going quiet is an ESCALATION, not an explanation" — did **not**
  trigger; narrowing the attempt fence did not silence the mutation that exists to catch its
  absence). All four conditions met.

## Rule-18 note on the reading

The per-job log API is a *different invocation* from the project's `gh run view --log` habit. It
reads the same runner log bytes (the `Classify` step's stdout), so the lines above are the lines the
workflow wrote — but it is recorded as the reading method so that a future discrepancy has a place
to start.
