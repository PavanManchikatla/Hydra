# Receipt — M3 gate row 14(a), network-jitter chaos arm · run 32612884434

**2026-08-23**, workflow `container-2node`, sha `f37bb93`. Verified by an **actual CI run**, not by
the presence of committed workflow code.

## netem genuinely installed (not merely requested)

```text
qdisc netem 8001: root refcnt 2 limit 1000 delay 50ms  20ms loss 1% reorder 25% 50% gap 1
```

## Quoted verdicts (rule 16 — the log, never the job status)

```text
verdict=GREEN (two-node docker-kill recovery held)
verdict=GREEN (two-node docker-kill recovery held under tc netem jitter)
```

The baseline run and the jitter run both produced `CONTAINER_2NODE_RECOVERY_OK`, so the recovery
assertion held **with and without** 50 ms ± 20 ms of normally-distributed delay, 1 % loss and 25 %
reordering on the container bridge.

## Scope, annotated rather than implied

* **Container-CI-only.** `tc netem` needs `NET_ADMIN` and a real network namespace. The owner's Mac
  cannot inject it natively, and the local-pair is loopback inside one process — the Linux
  containers of this workflow are the arm's only home.
* **Correctness-only.** This proves jitter may degrade latency but **never changes what the pipeline
  computes**. It is **not** a latency measurement, and nothing here may be cited as one. The
  wired-LAN performance envelope remains separately owed and hardware-contingent (gate row 15).
