# Receipt — M1 FULL FLIP · the complete five-criteria set · 2026-08-23

**Model:** `HydraActivationCore.tla` at sha **`f8badc3`**, verified unchanged through HEAD `4e6c6c4`
(`git log f8badc3..HEAD -- verification/HydraActivationCore.tla verification/*.cfg` is empty).
Every verdict below is from a run of **that** model — no result is carried over from a superseded
version, and no equivalence argument is substituted for a receipt.

## Criterion 1–4 — standing bounds, run 32605432737 (sha `f8badc3`)

`BaselineSafety.cfg` / `BaselineLiveness.cfg` / `Mut1Unservable.cfg` / `Mut3AttemptFence.cfg` /
`Mut3Fast.cfg` / `BaselineSafetyFast.cfg` at the FULL standing bounds
(`Stages={s1,s2}`, `MaxEpoch=1`, `MaxRId=1`, `MaxAttempt=2`, `MaxPos=1`, `MaxCrashes=2`,
`|msgs| ≤ 20`, `MaxCkpt=3`):

```text
config=baseline-live expect=clean violated=0 complete=1 verdict=GREEN
config=baseline-safety expect=clean violated=0 complete=1 verdict=GREEN
config=baseline-safety-fast expect=clean violated=0 complete=1 verdict=GREEN
config=mut1 expect=violation violated=1 complete=0 verdict=GREEN
config=mut3 expect=violation violated=1 complete=0 verdict=GREEN
config=mut3-fast expect=violation violated=1 complete=0 verdict=GREEN
```

## Criterion 5 — Mut2 and Mut4, run 32605434178 (sha `f8badc3`)

```text
config=mut2 msgs=20 crashes=2 attempt=2 expect=violation violated=1 complete=0 verdict=GREEN
config=mut2 msgs=8 crashes=2 attempt=2 expect=violation violated=1 complete=0 verdict=GREEN
config=mut4 msgs=20 crashes=2 attempt=2 expect=violation violated=1 complete=0 verdict=GREEN
config=mut4 msgs=8 crashes=2 attempt=2 expect=violation violated=1 complete=0 verdict=GREEN
```

Corroborated locally on the same model (rule-7-permitted set): `Invariant CaseBPure is violated`
(834 distinct) and `Invariant AbortFinality is violated` (18 637 distinct).

## What is now true, and what is not

**True:** the transition core is **model-checked to FIXPOINT at the standing bounds** — safety and
liveness both `complete=1`, the first time either has ever drained there — and **all four mutations
fire** on the same model at the same bounds. Six implementation mutations remain **200/200** in the
randomized simulator.

**Not claimed:** this is a **bounded** model check. `MaxEpoch=1`, `MaxRId=1`, `MaxAttempt=2`,
`MaxPos=1`, `MaxCrashes=2`, `|msgs| ≤ 20`, `MaxCkpt=3` — the bounds are part of the claim and are
stated with it. Liveness holds **under per-message-class weak fairness**, which is a modelling
assumption about the environment, not a theorem about the network.

## Prior history preserved, not rewritten

The pre-repair run **32604092015** (sha `5a18049`) also returned standing-bounds
`baseline-live … verdict=GREEN`. It is recorded as corroboration and was **deliberately not used as
the flip receipt**: it predates the Mut3 consistency repair, and although that repair is provably
inert for `AttemptFencing=TRUE` configs, an equivalence argument is not a receipt.
