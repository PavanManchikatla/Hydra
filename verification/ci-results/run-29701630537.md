# CI receipt — run 29701630537 (TLC `long` matrix, workflow_dispatch `recover=true`)

**Read 2026-07-19 per rule 16 (verdicts are the `config=… verdict=` Classify lines, NEVER the job ✓/X).**
Dispatched to continue the 07-13 banked checkpoints via the §7.17-fixed `-recover` path.

## Verbatim Classify verdicts

```
config=baseline-live expect=clean   violated=0 complete=0 verdict=INCONCLUSIVE
config=mut3          expect=violation violated=0 complete=0 verdict=INCONCLUSIVE
```

- **baseline-live**: `verdict=INCONCLUSIVE` (violated=0, complete=0) — resumed then time-boxed; **NOT a clean drain**.
- **mut3**: `verdict=INCONCLUSIVE` (violated=0, complete=0) — ran 5h35m, **did NOT fire** (no counterexample); ~610.2M distinct / ~391K states still on queue. NOT a clean drain either → the drain-clean contingency ladder is **NOT** triggered (a time-box, not a `complete`).
- **baseline-safety-fast**, **mut3-fast** (the §7.16 fast lane): job status **X (failure)** — no `verdict=GREEN` produced; consistent with the §7.17 note that `mut3-fast` is not actually shallow at `|msgs|≤20`. Not green.

**Consequence:** no leg produced `verdict=GREEN` (or `VIOLATION`/`complete`). **M1 full-flip STILL BLOCKED** (§0e mechanical gate: needs baseline-safety→fixpoint + baseline-live→clean drain + Mut3→fire). M1 stays **PASSED-code-side**. Nothing flipped.

## `-recover` cross-run round-trip — STRONGLY EVIDENCED (strict marker confirmation pending)

Both big legs printed the resume decision (not a FRESH restart):

```
RECOVER-DECISION: RESUMING from -recover states/26-07-13-03-37-31   (baseline-live)
RECOVER-DECISION: RESUMING from -recover states/26-07-13-03-37-32   (mut3)
```

Corroboration that this is a *genuine* resume, not a silent fresh start: the mut3 leg's earliest visible progress is `Progress(1587) … 610,193,591 distinct states found` — i.e. it **continued** the 07-13 banked ~610M-state space rather than restarting from 0. This is the first time the cross-run `-recover` chain (broken 3× — §7.14 misrecording, §7.15 unwired fetch, §7.16/§7.17 double-nest) has been observed resuming.

**Honesty caveat (observe-don't-assume, §7.12):** TLC's own `Starting recovery from checkpoint` line was **not positively located** in the fetched (large, possibly tail-truncated) `gh run --log` output. The RECOVER-DECISION + Progress(1587)/610M evidence is strong but the strict marker is unconfirmed here — so the §8 `-recover` owed infra-test is recorded **strongly-evidenced, strict-marker-confirmation pending** a targeted per-job log fetch, not definitively closed.
