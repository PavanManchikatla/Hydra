# External security & penetration audit — 2026-08-23

**Scope:** full repository, against the M4·1-hardened core (sha `581da25`).
**Result: 4 critical · 21 high · 20 medium · 12 low.** Findings **ACCEPTED as a body** by the design
authority.

> **⚠️ PROVENANCE OF THIS FILE — read before citing it.** This is **not the auditor's report**. The
> agent was given the findings as a **design-authority directive** summarising them, not the
> auditor's own prose, and this file is that directive transcribed into a working ledger. It is
> therefore **complete for the findings named in the directive and silent on everything else** — of
> the 57 findings, the directive names 4 criticals, 12 highs, 15 mediums and 1 low by ID with enough
> detail to act on; **the remainder are known only as counts.** Rows below marked *"named only by
> count"* are placeholders that must be filled from the auditor's document.
>
> **✅ THE AUDITOR'S ORIGINAL REPORT NOW ACCOMPANIES THIS FILE (2026-08-23):**
> **`verification/audit-2026-08-23-AUDITOR-REPORT.md`** — verbatim, all 57 findings with severity,
> confirmation status, file:line, exploit and fix. **Nothing is "named only by count" any more.**
>
> **Read that file as the primary source and this one as a secondary reading of it.** They agree on
> the wave grouping (the auditor's own closing recommendations *are* these waves). They differ in
> the per-finding **glosses**, and the reconciliation below records where — because a summary that
> renames a finding turns *"H7 is fixed"* into a claim about the summary rather than about the
> audit. **One genuine gap surfaced when the primary source was finally read: M7.** See §7.42.
>
> *(Original note, retained:)* The auditor's original report should replace or accompany this file.
> Until it does, a reader should treat this ledger as *what was acted on*, not as *what was found*
> — and those are not the same claim. Recording the difference is cheaper than discovering it later.

---

## Reconciliation — this directive's glosses vs the auditor's findings (2026-08-23)

| Wave | Auditor's IDs | The auditor's actual titles | This directive's gloss | Verdict |
|---|---|---|---|---|
| 1a | C1 · H14 · H13 | manifest key embedded · no model-identity binding · pre-alloc before signature | same | ✅ agree |
| 1b | C2 | mTLS identity discarded at accept | same | ✅ agree |
| 1c | C3 · M4 · H7 · M5 | position count from payload length · silent `i64→i32` narrowing · **out-of-vocab token id across `extern "C"`** · **no exception barrier at the 14 shim entry points** | shape cross-check · `n_positions == 1` · reject `n > n_batch` · `token_id < n_vocab` | ⚠️ **labels shuffled, work complete** — all four implemented (the "standalone directives" for `i32::try_from` and the 14 guards *were* M4 and M5) |
| 1d | C4 · M9 (+ H1–H5, H19, H20 as defence-in-depth) | data plane unfenced · duplicate FWD/APPLY_TOKEN applied twice | same | ✅ agree |
| 2 | H8 · H9 · H10 · M6 · **M7** | WAL mid-stream corruption · writer poisoning · durable recovery discard · UNSERVABLE durability · **`GENERATION_COMMIT` contiguity** | …· "M7 (contiguity)" | ⛔ **M7 aimed at the wrong record type** — boundary-store contiguity was implemented; the commit-stream half is still open (§7.42, §8) |
| 3 | H6 · H12 · H11 · H17 · H18 · M1 · M2 · M3 | *(now fully named — see the report)* | same set | ✅ agree |
| 4 | H15 · H16 · H21 · M16 | *(now fully named)* | same set | ✅ agree |
| 5 | M20 | checklist rows whose test proves a narrower property | same | ✅ agree |

**⚑ RULE-20 RE-VERIFICATION (2026-08-23, §7.47) — every Wave 1–3 finding re-read against the auditor's own text.** Four gloss drifts found, all now closed: **M7** (aimed at the wrong record type), **M11** (`chunks_exact` truncation still live), **M13** (the missing wire decode arm; its spec half was already correct per the H1/M13 amendment), **M5** (`text_len` narrowing). Everything else reconciles: C1–C4, H1–H5, H6–H14, H17–H20, M1–M4, M6, M8–M10, M12, M14–M20 match their gloss in substance or are correctly scheduled for a later wave. **H6's abort is not an upstream defect we found — it is a stale submodule pin (§7.48).**

**Mediums and lows no longer "named only by count":** M8 (WAL not session-bound), M10 (structured error
replies never emitted), M11 (`bytes_to_f32_le` truncation — **closed by C3's shape check**), M12
(**deterministic session ids** — *not* the `SessionKeys` rename this ledger recorded; the rename was
correct work under §7.35, but the auditor's M12 asks for **CSPRNG session ids and no `dev()` outside
`cfg(test)`**, which is open), M13 (**no `Body::ResetRecoveryAttempt` wire decode arm** — the spec
half was amended in Wave 1; the wire half is open), M14, M15, M17, M18, M19, and L1–L12.

---

## The central thesis — ratified as a standing lesson

> **The checklist's rows outrun their assertions.**

M4·1 built `docs/SECURITY-CHECKLIST.md` on the principle that *a documented invariant nothing checks
is not an invariant*. The audit accepted that and pushed it one level further: **a row that claims
more than its test proves is the same failure wearing the test's clothes.** `verify()` was the
example — the checklist row read "manifest signature verified before any shard weights are read",
and a test proved exactly that sentence, while the signature was checked against a key the attacker
supplies.

**Ratified as PROJECT_STATE §7.31:**

> **A test whose name promises more than its assertion is worse than no test, because it terminates
> inquiry.** A missing test leaves a visible hole. An over-promising test fills the hole with an
> answer, and the reader — reasonably — stops looking.

Wave 5 exists to walk every checklist row and either tighten the test to the row's claim or narrow
the row's language to what the test proves.

---

## Threat-model ruling (governs every severity below)

**v1 keeps the honest-worker assumption — but it is now an explicit, documented, load-bearing
assumption rather than something the identity story implies away.**

Landed in **BLUEPRINT §1.9** and the **spec's scope section**:

* mTLS + signed manifests protect against **outsiders and accidents**, not a **compromised stage**.
* A worker holding a valid cluster certificate can lie about its computation; v1 has no mechanism
  that detects it.
* What rests on the assumption is named: teacher-forced recovery trusts a survivor's replayed
  boundaries; the solver trusts reported capability; `BOUNDARY_COPY` durability trusts the copier;
  S_P is the single source of sampled tokens.
* **Worker-compromise resistance is v2 scope.**

**The criticals were fixed anyway** — none of them depends on worker trust. Each is reachable by an
outsider, a hostile file, or an accident.

---

## Remediation waves

| Wave | Scope | Status |
|---|---|---|
| **1** | C1+H14+H13 ✅ · C2 ✅ · **1c** C3+M4+H7+M5 ✅ · **1d** C4+M9+**H2**+**H3**+**H4**+**H19** ✅ | ✅ **WAVE 1 COMPLETE** (1a+1b+1c+1d) — Wave 2 next |
| 2 | Durability: H8, H9, H10, **H5**, M6, M7 | ✅ **COMPLETE** — **M7 corrected in Wave 4 seam 1 (§7.47)**; the Wave-2 pass had aimed it at the boundary store. Originally marked complete (§7.39–§7.40) — oracle-first: header-byte torn-write cases, a chaos sink that persists bytes on failure, and the sim's hard-coded fsync removed, *before* the fixes |
| 3 | Supply chain + platform: H6, H12, H11, H17, H18, **H20**, M1, M2, M3, **L1 (raised)** | ✅ **COMPLETE** (§7.43–§7.45) — H6's vendored-parser target **found a SIGABRT on its first sweep**; H12's generator cap was the reason the fuzzer could not see H12; the platform seven were one shared oracle failure (a well-behaved peer). Residuals named in §8: H18's structural half, M1's semaphore, L1's fork SHA, M3's revocation |
| 4 | HTTP, **before any binary serves it**: H15, H16, H21, M16 | ✅ **COMPLETE** (§7.47, §7.49) — seam 1 was the M7 correction; then H15, H16, M16 fixed and **H21 partial** (the false `https://` allow-list claim removed; the router still has no TLS layer, owed with M4·0, which is the first binary to serve it) |
| 5 | Remaining mediums + M20 — every checklist row tightened or narrowed | ⛔ |

**M4·2/3/4 stay blocked until Wave 1 lands.**

---

## Finding ledger

Disposition vocabulary: **fixed** · **accepted-with-assumption** (correct under the honest-worker
ruling, recorded not silently dismissed) · **deferred-with-reason**.

### Critical

| ID | Finding | Disposition | Where |
|---|---|---|---|
| **C1** | **Manifest signature verified against the key embedded in the manifest.** `Manifest::verify()` checked the signature against `self.signer_pubkey` — a self-attestation. Any attacker generates a keypair, signs a manifest describing any shard bytes, and it verifies; every downstream check then validates the attacker's own numbers | **FIXED** (Wave 1a) | §7.32 · `verify_against(trusted)`, argument-less form removed; `TrustedSigner` + `ShardManifestConfig` make the anchor structural. Regression: `shard_anchor.rs::a_manifest_signed_by_an_untrusted_key_is_refused` |
| **C2** | **No peer identity is bound to a role.** The mTLS handshake authenticates *that* a peer is in the cluster, never *which* stage it is; any certificate holder may send any message family | **FIXED** (Wave 1b) | §7.34 · `roles::RoleTable` binds SAN→role at `accept()`, fail-closed on none/unparseable/ambiguous; `AcceptedConn` makes serving without a role unrepresentable; an allow-list message-family gate runs on a pre-decode peek. Regressions: `hydra-transport/tests/role_binding.rs` (6, on a **real** listener) + `reserved_hooks.rs` (3) |
| **C3** | **Boundary tensor length is never cross-checked against its declared shape** before crossing the FFI | **FIXED** (Wave 1c) | §7.37 · `wire::check_boundary_tensor` proves `dims == [n_positions, n_embd]` ∧ `data.len() == n_positions × 4 × n_embd` (engine-free, before the copy), `Msg::Fwd`/`BoundaryCopy` carry the declared shape, `worker::Engine::apply_boundary` proves declared `n_embd == engine.n_embd` before `hydra_apply`; `hydra_engine_sys::Context::apply_boundary` takes `n_positions` **explicitly** (never `len / n_embd`); the same check on the disk-side `decode_boundary_record` + a 7th fuzz target `boundary-record` (rule 17). **The enshrining control in `wire_limits.rs::an_oversized_position_count_is_refused` rewritten; the blind oracle named in the test's doc-comment.** Regressions: `wire_limits.rs` (+3), `pre_ffi.rs` (4, engine-gated), `fuzz_smoke.rs` (+1) |
| **C4** | **The data plane is unfenced.** Epoch / generation / serve-eligible state are checked on control frames, not on `APPLY_TOKEN`/`FWD` | **FIXED** (Wave 1d) | §7.38 · `worker::Worker::data_plane_fence` — `session_epoch` must match, non-zero `stage_generation` must match, and serve-eligibility follows the frame **class** per spec §1.1 F1: `SAMPLE` (decode) requires `ACTIVE_FINAL`, `NO_SAMPLE` (teacher-forced / `REBUILD_APPLY`) is admitted in the rebuild states, **`PREACTIVE` and `LOST` serve neither** (I20). Reply is `ERR_FENCED`. **The harness had to learn to activate:** every driver in `pair.rs` and the recovery tests served from stages that were never activated, and two tests froze only *some* survivors of a session-wide epoch transition. Regressions: `idempotency.rs::{a_stale_epoch_data_plane_frame_is_fenced_not_applied, serving_eligibility_follows_the_frame_class}` |

### High — named in the directive

| ID | Finding | Wave |
|---|---|---|
| **H6** | Verify-over-fd + `O_NOFOLLOW`; **fuzz the vendored parser — the 24-CPU-hour budget currently protects the wrong parser** | 3 |
| **H7** | Reject `n > n_batch` before the FFI | **FIXED** (1c) — three layers: `worker::Engine::apply_boundary` (unreachable from the wire once M4 holds), `hydra_engine_sys::Context::apply` (`n ∈ [1, n_batch]`, `pos0 + n ≤ n_ctx`, buffers exactly `n × n_embd`), and the shim. `pre_ffi.rs::a_position_count_above_n_batch_is_refused_by_the_wrapper_before_the_shim` |
| **H8** | Mid-stream corruption must be refused, with resync | **FIXED** (2) — a corrupt frame *header* made the scanner conclude "nothing valid follows" and silently discard every record after it (one flipped magic byte lost 3 of 5 durable records, and `open_append` would have truncated them away). Resync walks 8-aligned offsets probing for the magic; any checksum-valid record after the damage ⇒ refuse; the discardable tail is bounded to one max record; probes capped, and exceeding the cap refuses |
| **H9** | Writer poisoning on I/O error | **FIXED** (2) — with a sink that persists bytes *then* fails, the writer accepted the next append and placed a valid record behind a partial one, manufacturing the corruption H8 refuses to open. `WalWriter` + `CommitStream` poison on the first I/O error and refuse later appends without touching the sink; `CommitError::Poisoned` is distinct from a fresh I/O error |
| **H10** | Durable recovery discard + `CommitStream::open` | **FIXED** (2) — `open` did not exist, so the restart path had no oracle at all; a resumed coordinator would have re-used commit ids, accepted a backwards input frontier and re-opened the emit-after-commit gate. Restores all four watermarks; discard is truncate + `fdatasync` before returning; a damaged ledger refuses to reopen |
| **H2 ✅ FIXED (1d)** — `StageEvent::RecvFinalize` now carries the **epoch** and the SM checks it (§7.38). **The code was strictly weaker than the model it mirrors:** `StageRecvFinalizeAt` has always required `m.tgt = stEpoch[s]`, so TLC could never exhibit the defect. Regression: `hydra-state/tests/stage.rs` + `recovery.rs`. **`completion_id`/`complete_record_hash` are still dropped at decode** — carrying them needs the coordinator's durable `ACTIVATION_COMPLETE` record to compare against, which is the coordinator driver's work; **named as owed in §8, not silently closed.** | **`FINALIZE_ACTIVATION` accepted with no completion evidence.** `completion_id` / `complete_record_hash` are **dropped at decode**; the stage checks only `state == Preactive && attempt == self.attempt` — **no epoch, no `recovery_id`** (the model checks `m.tgt = stEpoch[s]`). Yields a stage-side **I25 violation** and a later Case-B′ fatal audit event | **1d** — post-C2 the *forgery* path drops to defense-in-depth, but **a legitimate coordinator's stale finalize still lands**: the missing epoch check and the dropped evidence fields are independently reachable |
| **H3 ✅ FIXED (1d)** — `target == base + 1` and `0 ≤ truncate_to < n_ctx`, checked **before any state is touched** (§7.38); an out-of-range frame is dropped (→ `ERR_TRANSITION`, Case C), never clamped. Same shape as H2: `SendBeginRecovery` emits `base |-> recTarget - 1` by construction, so the model never generates the frame. Three harnesses were sending the degenerate `base = target = 0`. | **A `BEGIN_RECOVERY` freezes a serving stage and wipes KV.** Case A checks only `epoch == base`; **`target` is not required to be `base+1`**; **`truncate_to` is unbounded and accepts negatives** → `kv_truncate` discards everything | **1d**, folded with M4 — forgery closed by C2; the missing `target == base+1` and `0 ≤ truncate_to < n_ctx` bounds are independently reachable |
| **H4 ✅ FIXED (1d)** — `hydra_state::AuthenticatedRank`: private field, no constructor in `hydra-state`, minted **only** by `hydra_transport::roles::BoundPeer::authenticated_rank()` from the C2 role binding; `CoordEvent::Stage{Committed,Finalized,Lost}` take it. A non-stage peer yields `None`, not rank 0. Done **explicitly**, as the audit asked. Regression: `role_binding.rs::{a_quorum_rank_can_only_be_minted_from_an_authenticated_stage_peer, the_activation_acks_carry_no_rank_on_the_wire}`. | **The activation quorum counts self-reported ranks.** `ACTIVATION_COMMITTED` carries **no rank at all** on the wire, so one peer can forge a quorum. The model gets one-ack-per-stage free from set semantics — a modelling artifact, not a protocol guarantee | **1d** — substantially closed **if** the ack set is keyed on the **authenticated peer identity**; do that **explicitly**, not implicitly. Auditor marked SUSPICIOUS (no production coordinator driver exists yet): a **latent SM defect the driver will inherit** |
| **H5 ✅ FIXED (2)** — the drivers fed one boundary per position in order, under which a `max()` frontier and a contiguous frontier are **the same function**; the difference is that `max()` acks durability over a hole and R3′ then frees the boundary a D1 rebuild replays. Now: contiguous frontier, idempotent duplicates, (cluster, session, epoch) fence on stored records, poisoning on I/O error, and the `unwrap_or(-1)` swallows removed from the serve loops. | **Durability plane.** Unauthenticated boundary writes; **`max()`-based frontier**; **swallowed append errors** (`unwrap_or(-1)`, loop continues) ⇒ **acking durability over holes**; no dedupe by position; no session/epoch fence on stored records | **2** — the unauthenticated-writer half closes with C2; the swallowed error, max-frontier and dedupe defects are **pure implementation bugs, independently reachable**, and belong with H8–H10 |
| **H11** | *(named only by ID in the directive)* | 3 |
| **H12** | *(named only by ID)* | 3 |
| **H19 ✅ FIXED (1d)** — `goal ∈ [0, n_ctx]` refused **before** the loop, and the loop runs only while the stage is in a rebuild state, capped at `n_ctx + 1` iterations. Regression: `idempotency.rs::an_absurd_catch_up_goal_is_refused_immediately_rather_than_looped` (wall-clock oracle — a regression hangs rather than fails). | **`CATCH_UP_CONTEXT{goal}` loops `goal+2` times with no fence applied first** — `goal = i64::MAX − 2` **hangs the single-threaded worker** | **1d** — forgery closed by C2; still bound it (`goal ≤ n_ctx`, loop only while in a rebuild state). Two lines, and it removes a **self-DoS from an honest bug** |
| **H20** | **`sampled_ring` is unbounded**, keyed by attacker-chosen `output_pos`, ~1 MiB retained per `SAMPLE_NEXT`; the penalty window is re-emitted into every snapshot | **3** (hygiene) — drops to defense-in-depth post-C2. Bounds: evict below the committed watermark, require `output_pos == sampled_pos + 1` or an existing key, cap `penalty_window.len()` |
| **H13** | **Signature verified after structural parse** — attacker-directed parsing on unauthenticated bytes | **FIXED** (1a) |
| **H14** | **No binding of `blake3(manifest)` to the fence tuple's `manifest_hash`** — a genuine, cluster-signed manifest for a *different* model substitutes cleanly | **FIXED** (1a) |
| **H15**, **H16**, **H21** | HTTP surface | 4 |
| **H17**, **H18** | Platform | 3 |
| **H1** | **Attempt-fence bounds are a genuine spec silence** — the floor is one-sided (`m.a >= stAttempt[s]`), so a single frame carrying `u32::MAX` is accepted, **becomes the new floor, and permanently denies every legitimate future attempt** | ⏸️ **RATIFICATION** — amendment drafted: `verification/spec-amendments-H1-M13.md` |

**Filled 2026-08-23** — the design authority supplied H2–H5, H19 and H20 by content after the agent
declined to classify them from a count-only ledger. **That refusal was correct and the gap was the
directive's**, recorded here because it is the ledger's own §7.31 moment: guessing at a severity drop
for text nobody had read would have been a row claiming more than its evidence.
*Highs still named only by ID: H11, H12. 21 total.*

### Medium — named in the directive

| ID | Finding | Wave |
|---|---|---|
| **M1** | **`Conn::recv` reserves `payload_len + 32` before the payload arrives** — 12 bytes of header hold up to 64 MiB while a peer stalls the `read_exact`. Not the D2 class (the cap is real and pre-allocation), but an unbounded *hold*. **Ruled 2026-08-23: accepted-with-assumption for the malice case, but Wave 3 lands a read timeout + per-peer/global connection semaphore** — the timeout is what makes the hold bounded, and a stalled read is as easily an accident as an attack | 3 |
| **M2, M3** | Platform / supply chain | 3 |
| **M4** | `n_positions == 1` in v1 | **FIXED** (1c) — `wire::check_boundary_tensor`, both boundary bodies; `wire_limits.rs::more_than_one_position_per_boundary_is_refused_in_v1` |
| **M5** | `token_id < n_vocab` validated before it becomes durable | **FIXED** (1c) — `Session::push_sampled` refuses (`CommitError::TokenOutOfVocab`) before the group committer owns the token: no buffer entry, no append, no watermark move, no event (`session_http.rs::an_out_of_vocabulary_token_is_refused_before_anything_is_written`); `PieceSource::n_vocab` makes the bound structural; the generation pump ends loudly on it. Worker side: `Engine::apply_token` before the FFI (`pre_ffi.rs::an_out_of_vocabulary_token_is_refused_before_the_ffi`) and the shim |
| **M6** | Simulator fsync special case | **FIXED** (2) — the sim `fsync`'d UNSERVABLE/TERMINATE the instant they were written, keeping itself consistent with the SM's own mistake (recording the write as durable in the same step). No schedule could place a crash in the window. Both halves fixed; **the TLA+ model does not change** — `Wal(r) == wal ∪ {r}` already means *durably written*, so the code was violating the abstraction, not extending it |
| **M7** | Contiguity checks | **FIXED** (2) — with H5; enforced on write *and* on read, because the file is what a different process recovers from |
| **M9** | Per-position idempotency per I1/R2 | **FIXED** (1d) — `worker::data_plane_idempotency`: `p == frontier+1` applies; `p == frontier` re-sends the **cached** replies byte-for-byte (R2 forbids recomputation); anything else is `ERR_GAP` carrying the frontier. The frontier follows a `BEGIN_RECOVERY` truncation. Regressions: `idempotency.rs::{a_retransmitted_position_is_re_acked_without_being_applied_again (digest-compared against a clean run — the KV is the oracle, not the ack), a_skipped_position_is_refused_with_err_gap_and_the_stage_survives_it}`. **Limit stated:** a duplicate *older* than the last position is refused rather than answered, because answering would need either recomputation (forbidden) or an unbounded reply cache (the H20 shape). |
| **M13** | **Reset/attempt-floor gap — a genuine spec silence.** §6.4 says a reset increments `recovery_id` and says **nothing** about the attempt space; the TLA+ model quietly chose "preserve" (`UNCHANGED << attempt, stAttempt >>`) and is self-consistent, so TLC has nothing to report. A second implementation reading the same text could restart attempts at 0 — and since F2 scopes the stage floor to **(session, epoch)** and a reset does not change the epoch, **every post-reset activation would be fenced by the stage's own floor: two conforming implementations, one deadlock** | ⏸️ **RATIFICATION** — amendment drafted: `verification/spec-amendments-H1-M13.md` |
| **M16** | HTTP | 4 |
| **M20** | Checklist rows tightened or narrowed | 5 |

*Remaining mediums (20 total) named only by count.*

### Wave 1c directives that carry no finding ID

| Directive | Disposition |
|---|---|
| `i32::try_from` on every network-derived position | **DONE** (1c) — `worker::wire_pos` on `APPLY_TOKEN.input_pos`, `FWD.first_input_pos` (bounded `[0, n_ctx)`), and `BEGIN_RECOVERY.truncate_to` (`saturating_add(1)` then `try_from`; the full H3 bound is 1d). No `as i32` on a wire `i64` remains in `worker.rs`'s data path. `pre_ffi.rs::a_position_outside_the_context_or_i32_is_refused_before_the_ffi` |
| `try/catch(...)` on all 14 shim entry points | **DONE** (1c) — `HYDRA_GUARD_BEGIN/END` in `csrc/hydra_engine.cpp`, mapping any throw to the entry point's failure value. **Inspection row, not a proof row:** no test can provoke a llama.cpp throw on demand without a hostile GGUF (that is H6/Wave 3's vendored-parser fuzzing) |
| Audit sibling tests for the enshrining-control shape | **DONE** (1c) — `reserved_hooks.rs::fwd_with_tensor` builds a consistent `[1,4]`/16-byte/`n_positions=1` frame (clean); every encoder in the tree writes `n_positions = 1` with 2-D dims. One **adjacent** instance found and fixed: `docs/SECURITY-CHECKLIST.md` named `wire_limits.rs::a_real_boundary_wider_than_the_position_cap_still_decodes`, a test that had been renamed — a row pointing at a test that does not run (§7.31) |

### Low

| ID | Finding | Disposition |
|---|---|---|
| **L1** | **The layer-window patch exists only as an uncommitted submodule modification.** A clean checkout silently builds an **ABI-mismatched engine**, and CI never builds the real engine at all | **RAISED INTO WAVE 3** by ruling — this is a release-integrity defect, not a low. Fix: pin a fork SHA, or commit the patch application to the build |

*Remaining lows (12 total) named only by count.*

---

## Two findings that pause for ratification

Per rule 11, anything touching protocol semantics stops and proposes an amendment rather than
choosing. Both of these are **genuine silences in the spec that the audit correctly identified** —
not implementation bugs:

* **H1 — attempt-fence bounds.** The spec fences activation attempts by `activation_attempt_id` but
  never bounds it. A proposed amendment accompanies Wave 1's report.
* **M13 — reset/attempt-floor gap.** `RESET_RECOVERY_ATTEMPT` increments `recovery_id` but the spec
  does not say what happens to the attempt floor across a reset.

Neither is fixed unilaterally. The model, then the code, follow the spec — not the reverse.
