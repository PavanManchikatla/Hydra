\* Mut7 (spec v0.10.5, 2026-09-10): EmptyDiscards = FALSE — a survivor KEEPS its stale KV under an EMPTY
\* BEGIN_RECOVERY (or, equivalently, the coordinator never asked for EMPTY). The replacement downstream
\* applies positions the upstream never re-emitted at the new epoch: RelayedSourcing (I26) MUST fire.
\* Bounds = BaselineSafetyD0Fast (the control that must be clean).
SPECIFICATION Spec

CONSTRAINT
StateConstraint

CONSTANTS
Stages = {s1, s2}
MaxEpoch = 1
MaxRId = 1
MaxAttempt = 2
MaxPos = 1
MaxCrashes = 1
MaxCkpt = 3
EnableUnservable = TRUE
ResetTruncates = TRUE
AttemptFencing = TRUE
AbortGuardEnabled = TRUE
AbortTerminal = TRUE
RestartDerivesByMax = TRUE
DurabilityD0 = TRUE
EmptyDiscards = FALSE

INVARIANTS
Inv

PROPERTIES
ResetPreservesAttemptSpace
